/**
 * Adds two numbers together.
 * @param {number} a 
 * @param {number} b 
 * @returns {number}
 */
export function add(a, b) {
  return a + b;
}

export function isEven(n) {
  return n % 2 === 0;
}
